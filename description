It was observed that a user with the Ops role is able to access functionalities restricted to the Admin role by directly navigating to privileged endpoints (forceful browsing). The application does not enforce proper authorization checks on these endpoints, allowing unauthorized access to administrative features. As a result, the user is able to view and modify data that should only be accessible to Admin users.
